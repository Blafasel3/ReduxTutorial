import { MapValuesToStringPipe } from './map-values-to-string.pipe';

describe('MapValuesToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new MapValuesToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
